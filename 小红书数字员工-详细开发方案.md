# 小红书数字员工 —— 详细开发方案

> **版本**：V1.0  
> **日期**：2026-03-10  
> **状态**：可并行开发，任务已拆分

---

## 一、项目总览

### 1.1 核心定位
**小红书AI内容生产 + 手动确认发布工具**

### 1.2 设计原则
```
少量多次 + 人工审核 + 模型可选 + 双保险存储
```

### 1.3 MVP边界（第一阶段）
| 包含 | 不包含（Phase 2） |
|------|------------------|
| 单账号管理 | 多账号切换 |
| 手动确认发布 | 定时自动发布 |
| 内容生产+审核 | 数据回流分析 |
| 本地+云端存储 | 团队协作功能 |

---

## 二、任务拆分与并行路线

### 2.1 任务总览图

```
Week 1-2: 基础建设
├── 任务A: 项目脚手架 + 存储架构 (前端负责人)
├── 任务B: AI模型接入层 (后端/AI负责人)
└── 任务C: Playwright PoC验证 (自动化负责人)

Week 3-4: 核心功能开发
├── 任务D: 内容生产流水线 (前端负责人)
├── 任务E: 审核系统实现 (前端负责人)
└── 任务F: 发布链路开发 (自动化负责人)

Week 5-6: 联调与优化
├── 任务G: 端到端联调 (全员)
├── 任务H: 用户体验优化 (前端负责人)
└── 任务I: 稳定性优化 (自动化负责人)

Week 7-8: 测试与交付
├── 任务J: 内测与Bug修复 (全员)
└── 任务K: 文档与部署 (全员)
```

---

## 三、详细任务说明

### 任务A: 项目脚手架 + 存储架构
**负责人**: 前端开发  
**工期**: Week 1-2  
**依赖**: 无

#### 交付物清单

| 序号 | 交付物 | 验收标准 |
|------|--------|----------|
| A1 | Tauri+React项目脚手架 | 可打包桌面应用，热更新正常 |
| A2 | SQLite本地数据库设计 | 表结构完整，迁移脚本可用 |
| A3 | 本地文件系统封装 | 图片读写、草稿保存正常 |
| A4 | 云端同步模块(Supabase) | 双向同步，冲突解决策略明确 |
| A5 | 网络状态管理 | 断网提示、离线模式可用 |
| A6 | 防误删机制(回收站) | 软删除、30天清理、可恢复 |

#### 核心数据模型

```typescript
// 数据库表结构定义

// 内容表
interface Content {
  id: string;                    // UUID
  productDesc: string;           // 产品描述
  generatedTitle: string;        // AI生成标题
  generatedBody: string;         // AI生成正文
  generatedTags: string[];       // AI生成标签
  originalImagePath: string;     // 原图本地路径
  processedImagePath?: string;   // 处理后图片路径
  status: 'draft' | 'reviewing' | 'approved' | 'published' | 'failed';
  modelUsed: string;             // 使用的模型
  createdAt: number;             // 时间戳
  updatedAt: number;
  deletedAt?: number;            // 软删除标记
}

// 账号表
interface Account {
  id: string;
  platform: 'xiaohongshu';
  username: string;
  cookies: string;               // 加密存储
  status: 'active' | 'expired';
  lastLoginAt: number;
}

// 配置表
interface Config {
  key: string;
  value: any;
  // 包括：默认模型、审核模式、批量大小等
}
```

#### 存储API接口

```typescript
class StorageManager {
  // 内容CRUD
  async saveContent(content: Content): Promise<void>;
  async getContent(id: string): Promise<Content>;
  async listContents(status?: string): Promise<Content[]>;
  async softDeleteContent(id: string): Promise<void>;
  async restoreContent(id: string): Promise<void>;
  
  // 账号管理
  async saveAccount(account: Account): Promise<void>;
  async getActiveAccount(): Promise<Account | null>;
  
  // 配置管理
  async getConfig<T>(key: string): Promise<T>;
  async setConfig<T>(key: string, value: T): Promise<void>;
  
  // 同步
  async syncToCloud(): Promise<void>;
  async syncFromCloud(): Promise<void>;
}
```

---

### 任务B: AI模型接入层
**负责人**: 后端/AI开发  
**工期**: Week 1-2  
**依赖**: 无

#### 交付物清单

| 序号 | 交付物 | 验收标准 |
|------|--------|----------|
| B1 | 硅基流动SDK集成 | 支持DeepSeek-V3/V2.5、Qwen-Max |
| B2 | 文案生成服务 | 输入产品描述，输出标题+正文+标签 |
| B3 | 图片处理服务 | 输入原图+场景，输出处理后图片 |
| B4 | 模型切换机制 | 可动态切换模型，失败自动降级 |
| B5 | Prompt模板库 | 小红书风格Prompt，可扩展 |
| B6 | API成本统计 | 每次调用成本记录，便于核算 |

#### 模型配置

```typescript
interface ModelRegistry {
  copywriting: {
    'deepseek-v3': {
      id: 'deepseek-ai/DeepSeek-V3',
      costPer1K: 0.001,
      maxTokens: 4096,
      characteristics: 'balanced'
    },
    'qwen-max': {
      id: 'Qwen/Qwen2.5-72B-Instruct',
      costPer1K: 0.003,
      maxTokens: 8192,
      characteristics: 'creative'
    },
    'deepseek-v2.5': {
      id: 'deepseek-ai/DeepSeek-V2.5',
      costPer1K: 0.0005,
      maxTokens: 2048,
      characteristics: 'fast'
    }
  },
  imageProcessing: {
    'qwen-image-edit': {
      id: 'Qwen/Qwen2.5-VL-72B-Instruct',
      costPerImage: 0.29,
      supports: ['background-replace', 'style-transfer']
    }
  }
}
```

#### 文案生成API

```typescript
interface CopywritingRequest {
  productDesc: string;
  model: 'deepseek-v3' | 'qwen-max' | 'deepseek-v2.5';
  style: 'casual' | 'professional' | 'trendy';
  length: 'short' | 'medium' | 'long';
}

interface CopywritingResponse {
  title: string;
  body: string;
  tags: string[];
  modelUsed: string;
  cost: number;
  latency: number;
}

class CopywritingService {
  async generate(request: CopywritingRequest): Promise<CopywritingResponse>;
  
  // 重新生成（保留部分结果）
  async regenerate(
    original: CopywritingResponse,
    feedback: string
  ): Promise<CopywritingResponse>;
}
```

#### 图片处理API

```typescript
interface ImageProcessingRequest {
  imagePath: string;           // 本地图片路径
  scene: 'beach' | 'studio' | 'lifestyle' | 'minimal';
  preserveProduct: boolean;    // 是否保留产品主体
}

interface ImageProcessingResponse {
  outputPath: string;          // 处理后图片本地路径
  success: boolean;
  error?: string;
  cost: number;
  latency: number;
}

class ImageProcessingService {
  async process(request: ImageProcessingRequest): Promise<ImageProcessingResponse>;
  
  // 预览蒙版（让用户确认主体识别）
  async generateMask(imagePath: string): Promise<{
    maskPath: string;
    previewPath: string;
  }>;
}
```

---

### 任务C: Playwright PoC验证
**负责人**: 自动化开发  
**工期**: Week 1  
**依赖**: 无

#### 交付物清单

| 序号 | 交付物 | 验收标准 |
|------|--------|----------|
| C1 | 小红书登录PoC | Cookie持久化，可保持登录态7天+ |
| C2 | 图文发布PoC | 连续10次发布，成功率≥80% |
| C3 | 反爬策略测试 | 识别常见风控手段，有应对方案 |
| C4 | 失败处理机制 | 登录过期、验证码、限流等异常处理 |
| C5 | 可视化封装 | Playwright操作可在界面中展示 |

#### 核心流程验证

```python
# 发布流程PoC代码结构

class XiaohongshuPublisher:
    def __init__(self):
        self.browser = None
        self.page = None
    
    async def login_with_cookies(self, cookies: str) -> bool:
        """使用Cookie登录"""
        pass
    
    async def publish_note(self, content: Content) -> Result:
        """
        发布笔记
        返回: {success: bool, noteUrl?: string, error?: string}
        """
        # 1. 打开创作页面
        # 2. 上传图片
        # 3. 填写标题
        # 4. 填写正文
        # 5. 添加标签
        # 6. 点击发布
        # 7. 等待发布成功，获取笔记链接
        pass
    
    async def handle_verification(self) -> bool:
        """处理验证码/滑块"""
        # 检测到验证码时，通知用户人工处理
        pass
    
    async def check_login_status(self) -> bool:
        """检查登录态是否有效"""
        pass
```

#### 测试用例

| 用例 | 描述 | 通过标准 |
|------|------|----------|
| C-001 | 正常发布图文 | 成功，返回笔记链接 |
| C-002 | 连续发布10条 | 成功率≥80%，无封号 |
| C-003 | Cookie过期处理 | 正确识别，提示重新登录 |
| C-004 | 网络中断恢复 | 可重试，或保存状态 |
| C-005 | 发布频率限制 | 识别限流，建议等待时间 |

---

### 任务D: 内容生产流水线
**负责人**: 前端开发  
**工期**: Week 3-4  
**依赖**: A1, B1, B2, B3

#### 交付物清单

| 序号 | 交付物 | 验收标准 |
|------|--------|----------|
| D1 | 批量设置界面 | 数量、模型、风格可配置 |
| D2 | 文案生成界面 | 显示生成进度，可单条编辑/重生成 |
| D3 | 图片处理界面 | 支持上传、场景选择、蒙版预览 |
| D4 | 流水线状态机 | 各阶段状态正确流转，可回退 |
| D5 | 草稿自动保存 | 每5秒自动保存，断网可恢复 |

#### 界面组件清单

```
views/
├── BatchSetupView/           # 批量设置
│   ├── BatchSizeSelector     # 数量选择(1-20)
│   ├── ModelSelector         # 模型选择
│   ├── StyleSelector         # 风格选择
│   └── AuditModeSelector     # 审核模式选择
│
├── CopywritingView/          # 文案生成
│   ├── ProgressBar           # 总体进度
│   ├── ContentCardList       # 内容卡片列表
│   │   └── ContentCard       # 单条内容卡片
│   │       ├── TitleDisplay
│   │       ├── BodyDisplay
│   │       ├── TagList
│   │       ├── EditButton
│   │       ├── RegenerateButton
│   │       └── ApproveButton
│   └── BatchActionBar        # 批量操作栏
│
├── ImageProcessingView/      # 图片处理
│   ├── UploadZone            # 拖拽上传区
│   ├── SceneSelector         # 场景选择
│   ├── MaskPreview           # 蒙版预览
│   ├── ProcessProgress       # 处理进度
│   └── ImageResultGrid       # 结果网格
│
└── FinalReviewView/          # 最终审核
    ├── SideBySidePreview     # 文案+图片并排预览
    ├── FinalEditButton
    ├── SaveDraftButton
    └── PublishButton
```

---

### 任务E: 审核系统实现
**负责人**: 前端开发  
**工期**: Week 3-4  
**依赖**: D1, D2, D3

#### 交付物清单

| 序号 | 交付物 | 验收标准 |
|------|--------|----------|
| E1 | 文案审核界面 | 批量审核，支持编辑/重生成/删除 |
| E2 | 图片审核界面 | 效果预览，支持重试/手动上传 |
| E3 | 发布前确认界面 | 最终预览，编辑入口 |
| E4 | 审核状态管理 | 各条内容审核状态正确记录 |
| E5 | 快捷操作 | 批量通过、批量重生成 |

#### 审核流程状态机

```typescript
enum ContentStatus {
  DRAFT = 'draft',                    // 初始草稿
  COPYWRITING_GENERATING = 'copywriting_generating',
  COPYWRITING_DONE = 'copywriting_done',
  COPYWRITING_EDITED = 'copywriting_edited',
  IMAGE_PROCESSING = 'image_processing',
  IMAGE_DONE = 'image_done',
  IMAGE_FAILED = 'image_failed',      // 可降级为原图
  READY_FOR_PUBLISH = 'ready_for_publish',
  PUBLISHING = 'publishing',
  PUBLISHED = 'published',
  PUBLISH_FAILED = 'publish_failed',
  APPROVED = 'approved',              // 审核通过
  REJECTED = 'rejected'               // 审核不通过
}

// 审核动作
interface AuditAction {
  type: 'approve' | 'reject' | 'edit' | 'regenerate' | 'manual_upload';
  contentId: string;
  payload?: any;  // edit时的修改内容等
}
```

---

### 任务F: 发布链路开发
**负责人**: 自动化开发  
**工期**: Week 3-4  
**依赖**: C1, C2, A2

#### 交付物清单

| 序号 | 交付物 | 验收标准 |
|------|--------|----------|
| F1 | 发布服务封装 | 与Playwright解耦，支持测试模式 |
| F2 | 可视化发布窗口 | 用户可看到浏览器操作过程 |
| F3 | 发布前检查 | 账号状态、内容完整性检查 |
| F4 | 发布结果反馈 | 成功/失败，失败原因提示 |
| F5 | 发布历史记录 | 保存发布记录，便于追溯 |

#### 发布服务接口

```typescript
interface PublishService {
  // 检查发布条件
  async checkPrerequisites(content: Content): Promise<{
    canPublish: boolean;
    reasons?: string[];
  }>;
  
  // 执行发布（可视化模式）
  async publish(
    content: Content,
    options: {
      visible: boolean;  // 是否显示浏览器窗口
      onProgress: (step: string) => void;
    }
  ): Promise<{
    success: boolean;
    noteUrl?: string;
    error?: string;
    screenshot?: string;  // 失败时截图
  }>;
  
  // 测试模式（不真发，仅验证）
  async publishTest(content: Content): Promise<void>;
}
```

---

### 任务G: 端到端联调
**负责人**: 全员  
**工期**: Week 5  
**依赖**: D1-D5, E1-E5, F1-F5

#### 交付物清单

| 序号 | 交付物 | 验收标准 |
|------|--------|----------|
| G1 | 完整流程跑通 | 从设置到发布，全流程无阻断 |
| G2 | 数据流验证 | 各阶段数据正确传递、存储 |
| G3 | 错误场景测试 | 各环节失败可正确回退/提示 |
| G4 | 性能基准 | 5份内容生产<30秒，发布<60秒 |

---

### 任务H: 用户体验优化
**负责人**: 前端开发  
**工期**: Week 5-6  
**依赖**: G1

#### 交付物清单

| 序号 | 交付物 | 验收标准 |
|------|--------|----------|
| H1 | 加载状态优化 | Skeleton屏、进度百分比 |
| H2 | 错误提示优化 | 友好错误文案，解决建议 |
| H3 | 快捷键支持 | 常用操作支持键盘 |
| H4 | 新手引导 | 首次使用引导流程 |
| H5 | 响应式适配 | 不同窗口尺寸正常显示 |

---

### 任务I: 稳定性优化
**负责人**: 自动化开发  
**工期**: Week 5-6  
**依赖**: G1

#### 交付物清单

| 序号 | 交付物 | 验收标准 |
|------|--------|----------|
| I1 | 重试机制 | API失败自动重试3次 |
| I2 | 断点续传 | 大图片上传中断可续传 |
| I3 | 日志系统 | 操作日志、错误日志分类存储 |
| I4 | 性能监控 | 各阶段耗时统计 |
| I5 | 内存管理 | 长时间运行不泄漏 |

---

### 任务J: 内测与Bug修复
**负责人**: 全员  
**工期**: Week 7  
**依赖**: H1-H5, I1-I5

#### 交付物清单

| 序号 | 交付物 | 验收标准 |
|------|--------|----------|
| J1 | 内测版本 | 打包可安装应用 |
| J2 | 测试用例执行 | 全部P0用例通过 |
| J3 | Bug修复 | P0 Bug清零，P1 Bug<5 |
| J4 | 性能基准 | 启动<3秒，操作流畅 |

---

### 任务K: 文档与部署
**负责人**: 全员  
**工期**: Week 8  
**依赖**: J1

#### 交付物清单

| 序号 | 交付物 | 验收标准 |
|------|--------|----------|
| K1 | 用户手册 | 安装、使用、常见问题 |
| K2 | 开发文档 | 架构说明、API文档 |
| K3 | 部署脚本 | 一键打包、自动更新 |
| K4 | 发布版本 | Windows/Mac安装包 |

---

## 四、项目依赖图

```
Week 1-2: 基础建设
┌─────────┐   ┌─────────┐   ┌─────────┐
│ 任务A   │   │ 任务B   │   │ 任务C   │
│ 存储架构 │   │ AI接入  │   │ PoC验证 │
└────┬────┘   └────┬────┘   └────┬────┘
     │             │             │
     └─────────────┴─────────────┘
                   │
Week 3-4: 核心功能开发
                   ↓
     ┌─────────────┼─────────────┐
     ↓             ↓             ↓
┌─────────┐   ┌─────────┐   ┌─────────┐
│ 任务D   │   │ 任务E   │   │ 任务F   │
│ 生产流水线│   │ 审核系统 │   │ 发布链路 │
└────┬────┘   └────┬────┘   └────┬────┘
     │             │             │
     └─────────────┴─────────────┘
                   │
Week 5-6: 联调与优化
                   ↓
     ┌─────────────┼─────────────┐
     ↓             ↓             ↓
┌─────────┐   ┌─────────┐   ┌─────────┐
│ 任务G   │   │ 任务H   │   │ 任务I   │
│ 端到端  │   │ 体验优化 │   │ 稳定性  │
└────┬────┘   └─────────┘   └─────────┘
     │
     ↓
Week 7-8: 测试与交付
┌─────────┐   ┌─────────┐
│ 任务J   │   │ 任务K   │
│ 内测    │   │ 文档部署 │
└─────────┘   └─────────┘
```

---

## 五、关键会议节点

| 周次 | 会议 | 参与人 | 目标 |
|------|------|--------|------|
| Week 1 | Kickoff | 全员 | 对齐需求、确认分工 |
| Week 2 | 技术评审 | 全员 | 架构方案评审 |
| Week 3 | 中期检查 | 全员 | 进度同步、风险暴露 |
| Week 5 | 联调启动 | 全员 | 集成测试准备 |
| Week 7 | 内测准备 | 全员 | 内测版本评审 |
| Week 8 | 发布评审 | 全员 | 上线决策 |

---

## 六、风险与应对

| 风险 | 可能性 | 影响 | 应对 |
|------|--------|------|------|
| Playwright不稳定 | 中 | 高 | 预留手动发布降级方案 |
| AI效果不达预期 | 中 | 中 | 增加人工编辑入口，不作为 blocker |
| 云端同步延迟 | 低 | 中 | 本地优先策略，异步同步 |
| 开发进度延期 | 中 | 中 | Week 2检查点，必要时裁剪功能 |

---

## 七、验收标准汇总

### 7.1 功能验收
- [ ] 单账号登录/登出正常
- [ ] 批量内容生产（5-10份）流程完整
- [ ] 三级审核节点全部可用
- [ ] 手动确认发布成功率≥80%
- [ ] 本地+云端双保险存储正常
- [ ] 断网/异常恢复机制可用

### 7.2 性能验收
- [ ] 启动时间 < 3秒
- [ ] 5份内容AI生成 < 30秒
- [ ] 单条发布操作 < 60秒
- [ ] 内存占用 < 500MB

### 7.3 体验验收
- [ ] 新手可独立完成首次发布
- [ ] 错误提示清晰，有解决建议
- [ ] 关键操作有二次确认
- [ ] 进度可见，等待有反馈

---

## 附录

### A. 技术栈参考
- **桌面框架**: Tauri (Rust + WebView)
- **前端**: React + TypeScript + Tailwind CSS
- **状态管理**: Zustand
- **本地存储**: SQLite (via sql.js)
- **云端同步**: Supabase
- **AI接入**: 硅基流动 API
- **自动化**: Playwright

### B. 命名规范
- 文件: `kebab-case.ts`
- 组件: `PascalCase`
- 函数: `camelCase`
- 常量: `SCREAMING_SNAKE_CASE`
- 接口: `PascalCase` + `I`前缀可选

### C. 代码提交规范
```
feat: 新功能
fix: Bug修复
docs: 文档更新
style: 代码格式（不影响功能）
refactor: 重构
test: 测试相关
chore: 构建/工具相关
```

---

*文档版本: 1.0 | 最后更新: 2026-03-10*
